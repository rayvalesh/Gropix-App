package com.tc.utils.variables.interfaces;

import com.tc.utils.utils.helpers.JamunAlertDialog;

public interface JamunDialogInterface {
    interface OnClickListener {
        void onClick(JamunAlertDialog jamunAlertDialog);
    }

}
